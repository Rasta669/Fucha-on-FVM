function generateProtocol(protocol) {
  if (protocol < 0 || protocol > 3) throw new Error('Invalid protocol');
  return new Uint8Array([protocol]);
}

export var ID = generateProtocol(0);
export var SECP256K1 = generateProtocol(1);
export var Actor = generateProtocol(2);
export var BLS = generateProtocol(3);
//# sourceMappingURL=constants.js.map